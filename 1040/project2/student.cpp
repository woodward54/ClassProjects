#include <string>
using namespace std;

#include "student.h"

student::student() {
  firstName = "noName";
  lastName = "noName";
  id = -1;
  numOfCourses = 0;
  classification = -1;
  return;
}
