#include <string>
using namespace std;

#include "course.h"

course::course() {
  name = "noName";
  location = "noLocation";
  time = "noTime";
  id = -1;
  numOfStudents = 0;
  courseAvg = -1;
  return;
}
